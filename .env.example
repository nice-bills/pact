# Secrets — NEVER commit these
DEPLOYER_PRIVATE_KEY=0x...
MINIMAX_API_KEY=sk-...
MINIMAX_API_BASE_URL=https://api.minimax.io
MINIMAX_MODEL=MiniMax-M2.5

# Non-secret config (used by deploy scripts)
STATUS_L2_RPC=https://public.sepolia.rpc.status.network
STATUS_AGENT_ADDRESS=0x...

# Deployed addresses — see config/deployments.json (committed, safe to share)
