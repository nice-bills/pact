# Secrets — NEVER commit these
DEPLOYER_PRIVATE_KEY=0x...
MINIMAX_API_KEY=sk-...
MINIMAX_API_BASE_URL=https://api.minimax.io
MINIMAX_MODEL=MiniMax-M2.5

# Deployed addresses — see config/deployments.json (safe to commit)
